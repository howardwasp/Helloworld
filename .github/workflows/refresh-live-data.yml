name: Refresh live data snapshots

on:
  schedule:
    - cron: '15 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-live-data
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Fetch USGS, NWS, NHC, GDELT, OFAC, and IODA snapshots
        run: node scripts/refresh-live-data.mjs

      - name: Commit if changed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add public/data/live
          if git diff --staged --quiet; then
            echo 'No snapshot changes'
          else
            git commit -m "Refresh live data snapshots (USGS / NWS / NHC / GDELT / OFAC / IODA)"
            git push
          fi
